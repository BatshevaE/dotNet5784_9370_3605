<Window x:Class="PL.DatePickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
   	    DataContext="{Binding Clock, RelativeSource={RelativeSource Self}, Mode=TwoWay}"
        Title="DatePickerWindow" Height="200" Width="200">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="#FF1CB1D2" Offset="1"/>
                <GradientStop Color="White" Offset="0"/>
                <GradientStop Color="#FF94E9E5" Offset="0.518"/>
            </LinearGradientBrush>
        </Grid.Background>
        <DatePicker DisplayDateStart="{Binding Date,Mode=OneWay}" HorizontalAlignment="Center" VerticalAlignment="Center"  SelectedDateChanged="DatePicker_Select" BorderBrush="{x:Null}" Background="{x:Null}" Height="64" Width="144"/>

    </Grid>
</Window>
